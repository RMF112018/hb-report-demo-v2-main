# Responsibility Matrix

<div className="flex flex-col items-center justify-center min-h-[400px] space-y-6">
  <div className="p-6 bg-red-100 dark:bg-red-900/20 rounded-full">
    <Grid3X3 className="h-16 w-16 text-red-600 dark:text-red-400" />
  </div>

  <p className="text-lg font-semibold text-center max-w-4xl">
    **Responsibility Matrix** eliminates "who owns this?" delays in meetings by **providing centralized visibility into
    role assignments and accountability** across all project phases and cost codes.
  </p>

  <p className="text-center text-muted-foreground max-w-3xl">
    Instead of scattered assignment documents and unclear ownership boundaries, **all responsibilities are clearly
    defined and validated** with real-time updates tied directly to staffing and scheduling tools.
  </p>

  <div className="grid grid-cols-3 gap-8 mt-8">
    <div className="text-center space-y-3">
      <div className="text-3xl">👁️</div>
      <div className="font-medium">Centralized Visibility</div>
      <div className="text-sm text-muted-foreground">Clear view of who's assigned to what across all phases</div>
    </div>

    <div className="text-center space-y-3">
      <div className="text-3xl">🔍</div>
      <div className="font-medium">Assignment Validation</div>
      <div className="text-sm text-muted-foreground">Cross-validates clarity and flags gaps automatically</div>
    </div>

    <div className="text-center space-y-3">
      <div className="text-3xl">⚠️</div>
      <div className="font-medium">Conflict Detection</div>
      <div className="text-sm text-muted-foreground">
        Identifies overlapping responsibilities before they impact work
      </div>
    </div>
  </div>
</div>{" "}
